services:
  bunjang-mcp:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:8001:8000"
    environment:
      HOST: 0.0.0.0
      PORT: 8000
      BUNJANG_CACHE_TTL_SECONDS: 300
      ALLOWED_ORIGINS: https://*.lost.plus
